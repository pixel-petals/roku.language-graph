/**
 * md-mermaid.transform.mjs
 *
 * TODO: transform src/database's graph into Markdown with embedded
 * Mermaid diagrams (an alternative to the plain wiki pages in md.transform.mjs).
 */

/** @param {{nodes: object[], edges: object[]}} graph */
export function toMermaidMarkdown(graph) {
  throw new Error('not implemented');
}
